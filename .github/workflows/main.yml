name: Build PS3 PKG

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
  container:
  image: zeldin/ps3dev-docker:latest
    steps:
    - uses: actions/checkout@v4

    - name: Build with PSL1GHT
      run: |
        make

    - name: Upload PKG Artifact
      uses: actions/upload-artifact@v4
      with:
        name: CustomPlayerPKG
        path: ./*.pkg
